{
  "name": "blog",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	 "build": "rm -rf dist && parcel build ./index.html --public-url /maket",
    "deploy": "cp ./dist/index.html ./dist/404.html && gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^2.0.1"
  },
  "dependencies": {
    "parcel": "^1.12.3"
  }
}
